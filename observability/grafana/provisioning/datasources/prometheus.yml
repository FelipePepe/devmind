apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: devmind-prometheus
    url: http://devmind-prometheus:9090
    access: proxy
    isDefault: true
    jsonData:
      timeInterval: 15s
