global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: devmind
    static_configs:
      - targets: ['devmind-backend:3001']
    metrics_path: /api/metrics
    scheme: http
