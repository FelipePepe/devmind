FROM debian:trixie-slim

RUN apt-get update -qq \
    && apt-get install -y -q curl \
    && curl -fsSL https://deb.nodesource.com/setup_22.x | bash - \
    && apt-get install -y -q nodejs python3 make g++ \
    && rm -rf /var/lib/apt/lists/* \
    && corepack enable
